@model StokCore.EntityLayer.Concrete.AppUser
@using StokCore.WebUI.Resources
@inject LanguageService ShareLocalizer
@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="utf-8" />
    <title>@ShareLocalizer.GetLocalizeHTML("RegisterTitle")</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body class="bg-light">

    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-5 col-sm-8">
                <div class="card shadow-sm">
                    <div class="card-body p-4">
                        <h3 class="card-title mb-4 text-center">@ShareLocalizer.GetLocalizeHTML("RegisterTitle")</h3>

                        <form asp-action="Register" method="post">
                            <div class="mb-3">
                                <label asp-for="UserName" class="form-label">@ShareLocalizer.GetLocalizeHTML("UserNameLabel")</label>
                                <input asp-for="UserName" class="form-control" placeholder="@ShareLocalizer.GetLocalizeHTML("UserNamePlaceholder")" />
                            </div>
                            <div class="mb-3">
                                <label asp-for="Email" class="form-label">@ShareLocalizer.GetLocalizeHTML("EmailLabel")</label>
                                <input asp-for="Email" type="email" class="form-control" placeholder="@ShareLocalizer.GetLocalizeHTML("EmailPlaceholder")" />
                            </div>
                            <div class="mb-3">
                                <label asp-for="Password" class="form-label">@ShareLocalizer.GetLocalizeHTML("PasswordLabel")</label>
                                <input asp-for="Password" type="password" class="form-control" placeholder="@ShareLocalizer.GetLocalizeHTML("PasswordPlaceholder")" />
                            </div>
                            <button type="submit" class="btn btn-success w-100">@ShareLocalizer.GetLocalizeHTML("RegisterButton")</button>
                        </form>

                        <div class="mt-3 text-center">
                            <a asp-action="Login">@ShareLocalizer.GetLocalizeHTML("AlreadyHaveAccount")</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>